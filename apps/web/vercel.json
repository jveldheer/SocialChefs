{
  "buildCommand": "cd ../.. && pnpm install && pnpm build --filter=@ultimate-social-chef/web",
  "outputDirectory": ".next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "env": {
    "DEMO_MODE": "true",
    "DATABASE_URL": "file:./data/social-chef.db"
  }
}
